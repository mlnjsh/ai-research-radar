# AI Research Radar - Python dependencies
#
# Core scanning uses only the standard library (urllib, xml.etree).
# The packages below are optional but recommended.

# HTTP requests (optional, scanner uses urllib by default)
requests>=2.31.0

# OpenAI API for AI-powered summaries (optional)
openai>=1.0.0
